# CryptoScamReport — 技術設計文件

## Technical Design Document v1.4

> 免費、匿名的虛擬貨幣詐騙與駭客事件回報平台
> Free, anonymous cryptocurrency scam & hack incident reporting platform

---

## 1. 專案概述 | Project Overview

### 1.1 核心使命 | Mission

提供一個免費的公共平台，讓任何人都能輕易的回報虛擬貨幣詐騙與駭客攻擊事件，並查詢已知的詐騙錢包地址。同時開放 REST API 供第三方開發者整合，API 存取需註冊取得 API Key。

### 1.2 目標使用者 | Target Users

- 遭遇詐騙的受害者（回報事件）
- 交易前想查證地址安全性的一般用戶（網站搜尋）
- 開發者/錢包/DApp（透過 API 整合風險查詢）
- 資安研究人員與區塊鏈分析師（API 批量查詢）
- 媒體與執法機關（統計參考）

### 1.3 核心功能模組與存取權限

| 模組                      | 說明              | 網站前端        | Developer API |
| ------------------------- | ----------------- | --------------- | ------------- |
| **回報 Report**           | 提交詐騙/駭客事件 | 🔓 公開         | 🔑 API Key    |
| **搜尋 Search**           | 搜尋回報紀錄      | 🔓 公開         | 🔑 API Key    |
| **地址查詢 Address**      | 查詢地址風險      | 🔓 公開         | 🔑 API Key    |
| **儀表板 Dashboard**      | 統計圖表          | 🔓 公開         | 🔑 API Key    |
| **開發者後台 Dev Portal** | 管理 API Key      | 🔒 Passkey 登入 | —             |

### 1.4 版本變更摘要

| 項目     | v1.0     | v1.1         | v1.2 (當前)                |
| -------- | -------- | ------------ | -------------------------- |
| 網站前端 | 全部公開 | 搜尋需登入   | **全部公開**               |
| API 存取 | 無限制   | Session 驗證 | **API Key 驗證**           |
| 認證用途 | 無       | 網站登入     | **開發者註冊取得 API Key** |
| IP 記錄  | 雜湊     | 完整記錄     | **完整記錄**               |

### 1.5 雙層架構概念

```
┌─────────────────────────────────────────────────────────┐
│                     使用者存取方式                         │
├──────────────────────┬──────────────────────────────────┤
│                      │                                  │
│   🌐 網站前端          │   🔑 Developer API                │
│   (瀏覽器使用)         │   (程式化呼叫)                     │
│                      │                                  │
│   ✅ 全部功能公開       │   ✅ 相同資料與功能                │
│   ✅ 無需註冊          │   ❌ 需註冊取得 API Key            │
│   ✅ Turnstile 防濫用   │   ✅ API Key 認證                 │
│   ✅ IP Rate Limit     │   ✅ 更高的 Rate Limit             │
│                      │   ✅ JSON 回應                     │
│                      │   ✅ 批量查詢支援                   │
│                      │                                  │
│   適合：一般使用者      │   適合：開發者、錢包、DApp          │
│                      │                                  │
├──────────────────────┴──────────────────────────────────┤
│              共用 Cloudflare Workers 後端                  │
│              共用 D1 資料庫                                │
└─────────────────────────────────────────────────────────┘
```

---

## 2. 技術架構 | Technical Architecture

### 2.1 技術棧總覽 | Tech Stack

```
┌──────────────────────────────────────────────────────────────┐
│                    Cloudflare CDN                             │
│               (Global Edge Network)                          │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐      ┌───────────────────────────────────┐ │
│  │  Cloudflare   │      │   Cloudflare Workers              │ │
│  │  Pages        │─────▶│   (Hono Framework)                │ │
│  │  (Frontend)   │      │                                   │ │
│  │              │      │  內部路由（網站前端用）               │ │
│  │  React + Vite │      │  /api/reports     POST (Turnstile)│ │
│  │  TypeScript   │      │  /api/search      GET  (公開)     │ │
│  │  TailwindCSS  │      │  /api/address     GET  (公開)     │ │
│  │  i18n (zh/en) │      │  /api/stats       GET  (公開)     │ │
│  │              │      │                                   │ │
│  │              │      │  開發者路由（API Key 認證）          │ │
│  │  Dev Portal   │─────▶│  /v1/reports      POST (🔑)       │ │
│  │  (Passkey)    │      │  /v1/search       GET  (🔑)       │ │
│  │              │      │  /v1/address      GET  (🔑)       │ │
│  │              │      │  /v1/stats        GET  (🔑)       │ │
│  │              │      │  /v1/batch        POST (🔑)       │ │
│  │              │      │                                   │ │
│  │              │      │  認證路由（開發者後台）              │ │
│  │              │      │  /api/auth/*      Passkey          │ │
│  │              │      │  /api/keys/*      API Key 管理     │ │
│  └──────────────┘      └──────────┬────────────────────────┘ │
│                                   │                          │
│                    ┌──────────────┼──────────────┐           │
│                    │              │              │           │
│               ┌────▼───┐   ┌─────▼────┐  ┌──────▼──┐       │
│               │  D1    │   │   KV     │  │Turnstile│       │
│               │(SQLite)│   │ (Cache + │  │(CAPTCHA)│       │
│               │        │   │ Sessions │  │         │       │
│               │        │   │ + Rate)  │  │         │       │
│               └────────┘   └──────────┘  └─────────┘       │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 元件選擇理由 | Component Rationale

| 元件         | 選擇                       | 理由                                            |
| ------------ | -------------------------- | ----------------------------------------------- |
| 前端框架     | **React + Vite**           | 生態系成熟、Cloudflare Pages 原生支援、適合 SPA |
| 樣式         | **TailwindCSS**            | 快速開發、設計一致性、易於深色模式              |
| 國際化       | **react-i18next**          | 成熟 i18n 方案、支援動態切換                    |
| 圖表         | **Recharts**               | 輕量、React 原生、適合儀表板                    |
| 後端框架     | **Hono**                   | 專為 Workers 設計、輕量、TypeScript 原生        |
| 資料庫       | **Cloudflare D1**          | 零成本起步、邊緣低延遲、SQL                     |
| 快取/Session | **Cloudflare KV**          | 統計快取 + Session + Rate Limit 計數            |
| 人機驗證     | **Cloudflare Turnstile**   | 免費、隱私友善（網站前端用）                    |
| 開發者認證   | **WebAuthn (Passkey)**     | 無密碼、防釣魚、現代標準                        |
| API 認證     | **API Key (Bearer Token)** | 開發者友善、易於整合                            |

### 2.3 專案目錄結構 | Project Structure

```
cryptoscam-report/
├── frontend/                    # Cloudflare Pages
│   ├── src/
│   │   ├── components/
│   │   │   ├── layout/
│   │   │   │   ├── Header.tsx
│   │   │   │   ├── Footer.tsx
│   │   │   │   └── Layout.tsx
│   │   │   ├── report/
│   │   │   │   ├── ReportForm.tsx
│   │   │   │   ├── ChainSelector.tsx
│   │   │   │   ├── ScamTypeSelect.tsx
│   │   │   │   └── SuccessModal.tsx
│   │   │   ├── search/
│   │   │   │   ├── SearchBar.tsx
│   │   │   │   ├── SearchFilters.tsx
│   │   │   │   ├── ResultCard.tsx
│   │   │   │   └── AddressProfile.tsx
│   │   │   ├── dashboard/
│   │   │   │   ├── StatCards.tsx
│   │   │   │   ├── TrendChart.tsx
│   │   │   │   ├── TypePieChart.tsx
│   │   │   │   └── ChainBarChart.tsx
│   │   │   ├── dev-portal/              # 開發者後台
│   │   │   │   ├── PasskeyRegister.tsx
│   │   │   │   ├── PasskeyLogin.tsx
│   │   │   │   ├── ApiKeyManager.tsx    # API Key 建立/刪除/檢視
│   │   │   │   ├── UsageDashboard.tsx   # API 用量統計
│   │   │   │   └── ApiDocs.tsx          # 內嵌 API 文件
│   │   │   └── shared/
│   │   │       ├── Turnstile.tsx
│   │   │       ├── AddressInput.tsx
│   │   │       └── LoadingSpinner.tsx
│   │   ├── pages/
│   │   │   ├── HomePage.tsx            # 🔓 公開
│   │   │   ├── ReportPage.tsx          # 🔓 公開
│   │   │   ├── SearchPage.tsx          # 🔓 公開
│   │   │   ├── DashboardPage.tsx       # 🔓 公開
│   │   │   ├── AddressPage.tsx         # 🔓 公開
│   │   │   ├── DevPortalPage.tsx       # 🔒 開發者後台（Passkey 登入）
│   │   │   ├── DevRegisterPage.tsx     # 開發者註冊
│   │   │   ├── DevLoginPage.tsx        # 開發者登入
│   │   │   └── ApiDocsPage.tsx         # API 文件頁（公開）
│   │   ├── hooks/
│   │   │   ├── useApi.ts
│   │   │   ├── useAuth.ts             # 開發者登入狀態
│   │   │   ├── usePasskey.ts           # WebAuthn 操作
│   │   │   ├── useDebounce.ts
│   │   │   └── useLocalStorage.ts
│   │   ├── contexts/
│   │   │   └── AuthContext.tsx          # 開發者認證 Context
│   │   ├── i18n/
│   │   │   ├── zh-TW.json
│   │   │   ├── en.json
│   │   │   └── i18n.ts
│   │   ├── lib/
│   │   │   ├── api.ts
│   │   │   ├── webauthn.ts
│   │   │   ├── validators.ts
│   │   │   └── constants.ts
│   │   ├── types/
│   │   │   └── index.ts
│   │   ├── App.tsx
│   │   └── main.tsx
│   └── ...config files
│
├── worker/                      # Cloudflare Workers API
│   ├── src/
│   │   ├── index.ts             # Hono app 入口
│   │   ├── routes/
│   │   │   ├── internal/        # 內部 API（網站前端用，/api/*）
│   │   │   │   ├── reports.ts
│   │   │   │   ├── search.ts
│   │   │   │   ├── stats.ts
│   │   │   │   └── address.ts
│   │   │   ├── v1/              # Developer API（/v1/*，需 API Key）
│   │   │   │   ├── reports.ts
│   │   │   │   ├── search.ts
│   │   │   │   ├── stats.ts
│   │   │   │   ├── address.ts
│   │   │   │   └── batch.ts     # 批量查詢
│   │   │   ├── auth.ts          # Passkey 註冊/登入
│   │   │   └── keys.ts          # API Key CRUD
│   │   ├── middleware/
│   │   │   ├── apiKeyAuth.ts    # API Key 驗證中間件
│   │   │   ├── sessionAuth.ts   # Session 驗證（開發者後台）
│   │   │   ├── turnstile.ts
│   │   │   ├── rateLimit.ts
│   │   │   ├── cors.ts
│   │   │   └── sanitize.ts
│   │   ├── services/
│   │   │   ├── authService.ts
│   │   │   ├── sessionService.ts
│   │   │   ├── apiKeyService.ts  # API Key 生成/驗證/用量追蹤
│   │   │   ├── reportService.ts
│   │   │   ├── searchService.ts
│   │   │   ├── statsService.ts
│   │   │   └── cacheService.ts
│   │   ├── db/
│   │   │   ├── schema.sql
│   │   │   └── migrations/
│   │   ├── types/
│   │   │   └── index.ts
│   │   └── utils/
│   │       ├── address.ts
│   │       ├── crypto.ts
│   │       └── validators.ts
│   ├── wrangler.toml
│   ├── tsconfig.json
│   └── package.json
│
├── shared/
│   ├── types.ts
│   ├── constants.ts
│   └── validators.ts
│
├── .github/
│   └── workflows/
│       ├── ci.yml               # PR 檢查（lint/typecheck/test/build）
│       └── deploy.yml           # 自動部署（develop→Preview, main→Production）
│
├── .husky/
│   └── pre-commit               # lint-staged hook
│
├── prettier.config.js           # 共用 Prettier 設定
├── package.json                 # Monorepo root scripts
└── README.md
```

---

## 3. 認證與 API Key 系統 | Auth & API Key System

### 3.1 架構總覽

```
┌──────────────────────────────────────────────────────────────┐
│                        存取模式                               │
├─────────────────────────────┬────────────────────────────────┤
│                             │                                │
│  🌐 網站前端使用者            │  🔑 API 開發者                  │
│                             │                                │
│  無需任何認證                 │  Step 1: Passkey 註冊帳號       │
│  Turnstile 防機器人           │  Step 2: 在後台建立 API Key    │
│  IP Rate Limit               │  Step 3: 使用 API Key 呼叫 API │
│                             │                                │
│  呼叫 /api/* 端點            │  呼叫 /v1/* 端點               │
│                             │  Header: X-API-Key: csr_xxx    │
│                             │                                │
└─────────────────────────────┴────────────────────────────────┘
```

### 3.2 API Key 格式

```
前綴        隨機部分（32 bytes hex）
  │         │
  ▼         ▼
csr_a1b2c3d4e5f6...共64個hex字元

範例：csr_7f3a9b2c4d1e8f0a6b5c3d7e9f1a2b4c8d0e6f3a5b7c9d1e2f4a6b8c0d2e4f

說明：
- 前綴 "csr_" = CryptoScamReport
- 64 hex chars = 32 bytes 隨機數
- 儲存時只存 SHA-256 hash（與密碼相同原則）
- 建立時完整顯示一次，之後只顯示前 8 字元
```

### 3.3 開發者註冊與 API Key 流程

```
開發者                      網站                      Worker API              D1 / KV
  │                         │                          │                      │
  │  1. 訪問 /developers     │                          │                      │
  │     點擊「建立開發者帳號」  │                          │                      │
  ├────────────────────────▶│                          │                      │
  │                         │  2. Turnstile 驗證         │                      │
  │                         │  3. POST /auth/register    │                      │
  │                         │     /begin                 │                      │
  │                         ├─────────────────────────▶│                      │
  │                         │                          │  產生 challenge       │
  │                         │◀─────────────────────────┤                      │
  │  4. 生物辨識驗證          │                          │                      │
  │◀────────────────────────┤                          │                      │
  ├────────────────────────▶│                          │                      │
  │                         │  5. POST /auth/register    │                      │
  │                         │     /finish                │                      │
  │                         ├─────────────────────────▶│                      │
  │                         │                          │  儲存 user +          │
  │                         │                          │  credential           │
  │                         │                          ├─────────────────────▶│
  │                         │  6. 註冊成功 + Session      │                      │
  │                         │◀─────────────────────────┤                      │
  │  7. 進入開發者後台        │                          │                      │
  │     點擊「建立 API Key」  │                          │                      │
  ├────────────────────────▶│                          │                      │
  │                         │  8. POST /api/keys         │                      │
  │                         ├─────────────────────────▶│                      │
  │                         │                          │  生成 API Key         │
  │                         │                          │  儲存 hash            │
  │                         │                          ├─────────────────────▶│
  │                         │  9. 回傳完整 API Key        │                      │
  │                         │     ⚠️ 僅顯示這一次！         │                      │
  │                         │◀─────────────────────────┤                      │
  │  10. 複製 API Key        │                          │                      │
  │      開始使用 /v1/* API   │                          │                      │
  │◀────────────────────────┤                          │                      │
```

### 3.4 API Key 驗證流程

```
開發者應用程式                    Worker API                 D1
     │                            │                         │
     │  GET /v1/search?q=0xABC     │                         │
     │  Header: X-API-Key: csr_xxx │                         │
     ├───────────────────────────▶│                         │
     │                            │  1. 提取 X-API-Key       │
     │                            │  2. SHA-256(key)         │
     │                            │  3. 查詢 api_keys 表     │
     │                            ├────────────────────────▶│
     │                            │  4. 比對 hash            │
     │                            │     檢查 is_active       │
     │                            │     檢查 rate limit      │
     │                            │◀────────────────────────┤
     │                            │  5. 記錄用量             │
     │                            │     +1 daily_usage       │
     │  6. 回傳結果                │                         │
     │◀───────────────────────────┤                         │
```

### 3.5 Passkey (WebAuthn) 流程

#### 註冊流程

```
開發者                     前端                        Worker API                D1
  │                        │                            │                       │
  │  1. 點擊「建立帳號」     │                            │                       │
  ├───────────────────────▶│                            │                       │
  │                        │  2. POST /auth/register/    │                       │
  │                        │     begin                   │                       │
  │                        │     {display_name,           │                       │
  │                        │      turnstile_token}        │                       │
  │                        ├───────────────────────────▶│                       │
  │                        │                            │  3. 驗證 Turnstile     │
  │                        │                            │     產生 challenge     │
  │                        │                            │     暫存 KV (60s)     │
  │                        │  4. 回傳                    │                       │
  │                        │     CreationOptions         │                       │
  │                        │◀───────────────────────────┤                       │
  │  5. 生物辨識驗證         │                            │                       │
  │◀───────────────────────┤                            │                       │
  │  6. 完成                │                            │                       │
  ├───────────────────────▶│                            │                       │
  │                        │  7. POST /auth/register/    │                       │
  │                        │     finish {credential}     │                       │
  │                        ├───────────────────────────▶│                       │
  │                        │                            │  8. 驗證 credential   │
  │                        │                            │     寫入 users 表     │
  │                        │                            │     寫入 credentials  │
  │                        │                            │     建立 Session (KV) │
  │                        │                            ├──────────────────────▶│
  │                        │  9. Set-Cookie: __session   │                       │
  │                        │◀───────────────────────────┤                       │
  │  10. 導向開發者後台      │                            │                       │
  │◀───────────────────────┤                            │                       │
```

#### 登入流程

```
開發者                     前端                        Worker API             KV
  │                        │                            │                     │
  │  1. 點擊「登入」         │                            │                     │
  ├───────────────────────▶│                            │                     │
  │                        │  2. POST /auth/login/begin  │                     │
  │                        ├───────────────────────────▶│                     │
  │                        │  3. 回傳 RequestOptions      │                     │
  │                        │     (Discoverable mode)     │                     │
  │                        │◀───────────────────────────┤                     │
  │  4. 選擇 Passkey +      │                            │                     │
  │     生物辨識             │                            │                     │
  │◀───────────────────────┤                            │                     │
  │  5. 完成                │                            │                     │
  ├───────────────────────▶│                            │                     │
  │                        │  6. POST /auth/login/finish │                     │
  │                        │     {credential}            │                     │
  │                        ├───────────────────────────▶│                     │
  │                        │                            │  驗證 → Session     │
  │                        │                            ├────────────────────▶│
  │                        │  7. Set-Cookie: __session   │                     │
  │                        │◀───────────────────────────┤                     │
  │  8. 導向開發者後台       │                            │                     │
  │◀───────────────────────┤                            │                     │
```

### 3.6 Session 管理

```
Session 儲存於 Cloudflare KV（僅用於開發者後台）：

Key:    session:<token>
Value:  {
          "user_id": "usr_abc123",
          "created_at": "2025-01-15T08:00:00Z",
          "expires_at": "2025-01-22T08:00:00Z",
          "ip": "203.0.113.45",
          "user_agent": "Mozilla/5.0..."
        }
TTL:    7 天

Cookie 設定：
  Name:     __session
  Value:    <random 32-byte hex token>
  HttpOnly: true
  Secure:   true
  SameSite: Lax
  Path:     /api
  Max-Age:  604800 (7 天)
```

---

## 4. 資料庫設計 | Database Schema (D1)

### 4.1 ER Diagram

```
┌──────────────┐
│    users     │
├──────────────┤
│ id (PK)      │
│ display_name │
│ created_at   │       ┌────────────────────┐
│ updated_at   │       │   credentials      │
│              │◄──────┤ id (PK)            │
│              │       │ user_id (FK)       │      ┌────────────────────┐
│              │       │ credential_id      │      │     api_keys       │
│              │       │ public_key         │      ├────────────────────┤
│              │       │ sign_count         │      │ id (PK)            │
│              │       │ device_type        │      │ user_id (FK)  ─────┼──▶ users
│              │       │ transports         │      │ key_hash           │
│              │       │ created_at         │      │ key_prefix         │
│              │       └────────────────────┘      │ name               │
└──────┬───────┘                                   │ is_active          │
       │                                           │ daily_limit        │
       │                                           │ daily_usage        │
       │                                           │ last_used_at       │
       │                                           │ total_requests     │
       │                                           │ created_at         │
       ▼                                           └────────────────────┘

┌──────────────┐       ┌────────────────────┐       ┌──────────────────┐
│   addresses  │       │      reports       │       │   daily_stats    │
├──────────────┤       ├────────────────────┤       ├──────────────────┤
│ id (PK)      │◄──┐   │ id (PK)            │       │ id (PK)          │
│ chain        │   │   │ address_id (FK)    │       │ stat_date        │
│ address      │   └───┤ chain              │       │ total_reports    │
│ address_lower│       │ address            │       │ total_amount_usd │
│ report_count │       │ scam_type          │       │ chain_breakdown  │
│ total_lost   │       │ description        │       │ type_breakdown   │
│ risk_score   │       │ amount_lost        │       │ created_at       │
│ first_reported│       │ currency           │       └──────────────────┘
│ last_reported│       │ amount_lost_usd    │
│ created_at   │       │ evidence_url       │
│ updated_at   │       │ contact_chain_tx   │
└──────────────┘       │ reporter_ip        │
                       │ reporter_ip_hash   │
                       │ reporter_ua        │
                       │ source             │  ← 'web' | 'api'
                       │ api_key_id         │  ← 透過 API 回報時記錄
                       │ turnstile_verified │
                       │ status             │
                       │ created_at         │
                       └────────────────────┘
```

### 4.2 SQL Schema

```sql
-- ========================================
-- 使用者表（開發者帳號，Passkey 認證）
-- ========================================
CREATE TABLE IF NOT EXISTS users (
  id            TEXT PRIMARY KEY,              -- 'usr_' + nanoid(16)
  display_name  TEXT NOT NULL,                 -- 顯示名稱 or 自動生成
  created_at    TEXT DEFAULT (datetime('now')),
  updated_at    TEXT DEFAULT (datetime('now'))
);

-- ========================================
-- WebAuthn 憑證表
-- ========================================
CREATE TABLE IF NOT EXISTS credentials (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id         TEXT NOT NULL REFERENCES users(id),
  credential_id   TEXT NOT NULL UNIQUE,
  public_key      TEXT NOT NULL,
  sign_count      INTEGER DEFAULT 0,
  device_type     TEXT,
  backed_up       INTEGER DEFAULT 0,
  transports      TEXT,                         -- JSON array
  created_at      TEXT DEFAULT (datetime('now'))
);

CREATE INDEX idx_credentials_user ON credentials(user_id);
CREATE INDEX idx_credentials_cred_id ON credentials(credential_id);

-- ========================================
-- API Key 表
-- ========================================
CREATE TABLE IF NOT EXISTS api_keys (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id         TEXT NOT NULL REFERENCES users(id),
  key_hash        TEXT NOT NULL UNIQUE,          -- SHA-256(api_key)
  key_prefix      TEXT NOT NULL,                 -- 前 12 字元 "csr_a1b2c3d4" 用於顯示辨識
  name            TEXT NOT NULL DEFAULT 'Default', -- 開發者自訂名稱
  is_active       INTEGER DEFAULT 1,             -- 0=停用, 1=啟用
  daily_limit     INTEGER DEFAULT 1000,          -- 每日請求上限
  daily_usage     INTEGER DEFAULT 0,             -- 今日已用次數
  usage_reset_date TEXT,                         -- 用量重置日期 'YYYY-MM-DD'
  last_used_at    TEXT,
  total_requests  INTEGER DEFAULT 0,             -- 累計總請求數
  created_at      TEXT DEFAULT (datetime('now'))
);

CREATE INDEX idx_api_keys_user ON api_keys(user_id);
CREATE INDEX idx_api_keys_hash ON api_keys(key_hash);
CREATE INDEX idx_api_keys_active ON api_keys(is_active);

-- ========================================
-- 地址表
-- ========================================
CREATE TABLE IF NOT EXISTS addresses (
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  chain           TEXT NOT NULL,
  address         TEXT NOT NULL,
  address_lower   TEXT NOT NULL,
  report_count    INTEGER DEFAULT 0,
  total_lost_usd  REAL DEFAULT 0,
  risk_score      INTEGER DEFAULT 0,
  first_reported  TEXT NOT NULL,
  last_reported   TEXT NOT NULL,
  created_at      TEXT DEFAULT (datetime('now')),
  updated_at      TEXT DEFAULT (datetime('now')),
  UNIQUE(chain, address_lower)
);

CREATE INDEX idx_addresses_chain ON addresses(chain);
CREATE INDEX idx_addresses_lookup ON addresses(chain, address_lower);
CREATE INDEX idx_addresses_risk ON addresses(risk_score DESC);
CREATE INDEX idx_addresses_reports ON addresses(report_count DESC);

-- ========================================
-- 回報表（記錄完整 IP + 來源追蹤）
-- ========================================
CREATE TABLE IF NOT EXISTS reports (
  id                  INTEGER PRIMARY KEY AUTOINCREMENT,
  address_id          INTEGER NOT NULL REFERENCES addresses(id),
  chain               TEXT NOT NULL,
  address             TEXT NOT NULL,
  scam_type           TEXT NOT NULL,
  description         TEXT,
  amount_lost         REAL,
  currency            TEXT DEFAULT 'USD',
  amount_lost_usd     REAL,
  evidence_url        TEXT,
  contact_chain_tx    TEXT,

  -- 回報者追蹤
  reporter_ip         TEXT NOT NULL,
  reporter_ip_hash    TEXT NOT NULL,
  reporter_ua         TEXT,

  -- 來源追蹤
  source              TEXT DEFAULT 'web',        -- 'web' | 'api'
  api_key_id          INTEGER REFERENCES api_keys(id),  -- API 來源時記錄

  turnstile_verified  INTEGER DEFAULT 0,
  status              TEXT DEFAULT 'pending',
  created_at          TEXT DEFAULT (datetime('now'))
);

CREATE INDEX idx_reports_address ON reports(address_id);
CREATE INDEX idx_reports_chain ON reports(chain);
CREATE INDEX idx_reports_type ON reports(scam_type);
CREATE INDEX idx_reports_status ON reports(status);
CREATE INDEX idx_reports_created ON reports(created_at DESC);
CREATE INDEX idx_reports_ip ON reports(reporter_ip);
CREATE INDEX idx_reports_source ON reports(source);

-- ========================================
-- 每日統計快照表
-- ========================================
CREATE TABLE IF NOT EXISTS daily_stats (
  id                INTEGER PRIMARY KEY AUTOINCREMENT,
  stat_date         TEXT NOT NULL UNIQUE,
  total_reports     INTEGER DEFAULT 0,
  total_amount_usd  REAL DEFAULT 0,
  chain_breakdown   TEXT,
  type_breakdown    TEXT,
  created_at        TEXT DEFAULT (datetime('now'))
);

CREATE INDEX idx_stats_date ON daily_stats(stat_date DESC);
```

### 4.3 風險評分演算法

```
risk_score = min(100, base_score + frequency_bonus + amount_bonus + recency_bonus)

base_score       = report_count × 15       (每筆回報 +15，上限 60)
frequency_bonus  = 近 7 天有 3+ 筆回報 → +20
amount_bonus     = total_lost_usd > $10,000 → +10 ; > $100,000 → +20
recency_bonus    = 最後回報在 24 小時內 → +10

風險等級：
  0-25   → 🟢 Low
  26-50  → 🟡 Medium
  51-75  → 🟠 High
  76-100 → 🔴 Critical
```

---

## 5. API 設計 | API Endpoints

### 5.1 路由總覽

#### 內部 API（網站前端用，`/api/*`）

| Method | Endpoint                       | 說明     | 驗證          |
| ------ | ------------------------------ | -------- | ------------- |
| `POST` | `/api/reports`                 | 提交回報 | Turnstile     |
| `GET`  | `/api/search`                  | 搜尋     | IP Rate Limit |
| `GET`  | `/api/address/:chain/:address` | 地址詳情 | IP Rate Limit |
| `GET`  | `/api/stats/overview`          | 統計總覽 | IP Rate Limit |
| `GET`  | `/api/stats/trends`            | 趨勢     | IP Rate Limit |
| `GET`  | `/api/stats/breakdown`         | 分佈     | IP Rate Limit |

#### 開發者 API（`/v1/*`，需 API Key）

| Method | Endpoint                      | 說明         | 驗證       |
| ------ | ----------------------------- | ------------ | ---------- |
| `POST` | `/v1/reports`                 | 提交回報     | 🔑 API Key |
| `GET`  | `/v1/search`                  | 搜尋         | 🔑 API Key |
| `GET`  | `/v1/address/:chain/:address` | 地址詳情     | 🔑 API Key |
| `GET`  | `/v1/stats/overview`          | 統計總覽     | 🔑 API Key |
| `GET`  | `/v1/stats/trends`            | 趨勢         | 🔑 API Key |
| `GET`  | `/v1/stats/breakdown`         | 分佈         | 🔑 API Key |
| `POST` | `/v1/batch/addresses`         | 批量地址查詢 | 🔑 API Key |

#### 認證與管理 API

| Method   | Endpoint                    | 說明              | 驗證       |
| -------- | --------------------------- | ----------------- | ---------- |
| `POST`   | `/api/auth/register/begin`  | 開始 Passkey 註冊 | Turnstile  |
| `POST`   | `/api/auth/register/finish` | 完成 Passkey 註冊 | —          |
| `POST`   | `/api/auth/login/begin`     | 開始 Passkey 登入 | —          |
| `POST`   | `/api/auth/login/finish`    | 完成 Passkey 登入 | —          |
| `POST`   | `/api/auth/logout`          | 登出              | 🔒 Session |
| `GET`    | `/api/auth/me`              | 當前使用者        | 🔒 Session |
| `GET`    | `/api/keys`                 | 列出我的 API Keys | 🔒 Session |
| `POST`   | `/api/keys`                 | 建立新 API Key    | 🔒 Session |
| `DELETE` | `/api/keys/:id`             | 刪除 API Key      | 🔒 Session |
| `PATCH`  | `/api/keys/:id`             | 啟用/停用 API Key | 🔒 Session |
| `GET`    | `/api/keys/:id/usage`       | 查看用量統計      | 🔒 Session |

### 5.2 Developer API 使用範例

#### 認證方式

```bash
# 所有 /v1/* 請求需帶上 API Key
curl -H "X-API-Key: csr_7f3a9b2c4d1e8f0a..." \
     https://cryptoscamreport.pages.dev/v1/search?q=0xAbC123
```

#### GET /v1/search — 搜尋

```bash
curl -H "X-API-Key: csr_xxx" \
     "https://api.cryptoscamreport.com/v1/search?q=0xAbC&chain=ETH&limit=20"
```

**Response (200)：**

```json
{
  "success": true,
  "data": {
    "results": [
      {
        "address_id": 1,
        "chain": "ETH",
        "address": "0xAbC123...def456",
        "report_count": 12,
        "total_lost_usd": 85000,
        "risk_score": 78,
        "risk_level": "critical",
        "scam_types": ["rug_pull", "phishing"],
        "last_reported": "2024-12-15T08:30:00Z"
      }
    ],
    "pagination": {
      "page": 1,
      "limit": 20,
      "total": 156,
      "total_pages": 8
    }
  },
  "meta": {
    "rate_limit": {
      "daily_limit": 1000,
      "daily_remaining": 987,
      "reset_at": "2025-01-16T00:00:00Z"
    }
  }
}
```

#### POST /v1/batch/addresses — 批量查詢（開發者專屬）

```bash
curl -X POST \
     -H "X-API-Key: csr_xxx" \
     -H "Content-Type: application/json" \
     -d '{
       "addresses": [
         { "chain": "ETH", "address": "0xAbC123..." },
         { "chain": "BTC", "address": "bc1qxy..." },
         { "chain": "SOL", "address": "9WzDKp..." }
       ]
     }' \
     https://api.cryptoscamreport.com/v1/batch/addresses
```

**Response (200)：**

```json
{
  "success": true,
  "data": {
    "results": [
      {
        "chain": "ETH",
        "address": "0xAbC123...",
        "found": true,
        "risk_score": 78,
        "risk_level": "critical",
        "report_count": 12
      },
      {
        "chain": "BTC",
        "address": "bc1qxy...",
        "found": false,
        "risk_score": 0,
        "risk_level": "unknown",
        "report_count": 0
      },
      {
        "chain": "SOL",
        "address": "9WzDKp...",
        "found": true,
        "risk_score": 42,
        "risk_level": "medium",
        "report_count": 3
      }
    ],
    "queried": 3,
    "found": 2
  }
}
```

> 批量上限：一次最多 50 個地址，每個地址計為 1 次 API 呼叫

#### API Key 驗證失敗回應

```json
// 401 - 未提供 API Key
{
  "success": false,
  "error": {
    "code": "API_KEY_MISSING",
    "message": "API Key is required. Get one at https://cryptoscamreport.com/developers"
  }
}

// 401 - API Key 無效
{
  "success": false,
  "error": {
    "code": "API_KEY_INVALID",
    "message": "Invalid API Key"
  }
}

// 403 - API Key 已停用
{
  "success": false,
  "error": {
    "code": "API_KEY_DISABLED",
    "message": "This API Key has been disabled"
  }
}

// 429 - 超過每日限額
{
  "success": false,
  "error": {
    "code": "RATE_LIMIT_EXCEEDED",
    "message": "Daily API limit exceeded. Resets at 00:00 UTC.",
    "daily_limit": 1000,
    "reset_at": "2025-01-16T00:00:00Z"
  }
}
```

### 5.3 API Key 管理 API

#### POST /api/keys — 建立 API Key

**Request：**

```json
{
  "name": "My Wallet App"
}
```

**Response (201)：**

```json
{
  "success": true,
  "data": {
    "id": 1,
    "name": "My Wallet App",
    "key": "csr_7f3a9b2c4d1e8f0a6b5c3d7e9f1a2b4c8d0e6f3a5b7c9d1e2f4a6b8c0d2e4f",
    "key_prefix": "csr_7f3a9b2c",
    "daily_limit": 1000,
    "created_at": "2025-01-15T08:00:00Z",
    "warning": "請立即複製此 API Key，之後將無法再次查看完整內容。"
  }
}
```

> 每個使用者最多建立 5 個 API Key

#### GET /api/keys — 列出 API Keys

**Response (200)：**

```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "name": "My Wallet App",
      "key_prefix": "csr_7f3a9b2c",
      "is_active": true,
      "daily_limit": 1000,
      "daily_usage": 156,
      "total_requests": 12450,
      "last_used_at": "2025-01-15T10:30:00Z",
      "created_at": "2025-01-01T00:00:00Z"
    }
  ]
}
```

### 5.4 內部 API（網站前端用）

與 v1 端點回傳格式相同，但不含 `meta.rate_limit` 欄位。

#### POST /api/reports — 提交回報

**Request：**

```json
{
  "chain": "ETH",
  "address": "0xAbC123...def456",
  "scam_type": "rug_pull",
  "description": "項目方撤走流動性...",
  "amount_lost": 5000,
  "currency": "USDT",
  "evidence_url": "https://etherscan.io/tx/0x...",
  "turnstile_token": "0.XXXX..."
}
```

**Response (201)：**

```json
{
  "success": true,
  "data": {
    "report_id": 1234,
    "message": "回報已提交成功 | Report submitted successfully"
  }
}
```

### 5.5 錯誤回應格式

| HTTP Code | Error Code            | 說明                       |
| --------- | --------------------- | -------------------------- |
| 400       | `VALIDATION_ERROR`    | 輸入驗證失敗               |
| 401       | `API_KEY_MISSING`     | 未提供 API Key（/v1/\*）   |
| 401       | `API_KEY_INVALID`     | API Key 無效               |
| 401       | `UNAUTHORIZED`        | Session 無效（開發者後台） |
| 403       | `API_KEY_DISABLED`    | API Key 已停用             |
| 403       | `TURNSTILE_FAILED`    | 人機驗證失敗               |
| 404       | `NOT_FOUND`           | 資源不存在                 |
| 429       | `RATE_LIMIT_EXCEEDED` | 超過限額                   |
| 500       | `INTERNAL_ERROR`      | 伺服器錯誤                 |

---

## 6. 前端頁面設計 | Frontend Pages

### 6.1 設計風格

**主題：Law Enforcement × Cybersecurity Firm（執法單位 × 資安公司）**

> 參考風格：CrowdStrike、Chainalysis、Mandiant、FBI IC3

#### 色彩系統 (CSS Variables)

```
核心色盤：
--navy-950: #0a0e1a    ← 主背景（深海軍藍）
--navy-900: #0d1321    ← 卡片/面板背景
--navy-800: #131b2e    ← 輸入框背景
--navy-700: #1a2540    ← 邊框/分隔線
--navy-600: #243052    ← 次要邊框

文字色：
--slate-400: #8892a8   ← 次要文字/標籤
--slate-300: #a8b2c8   ← 一般文字
--slate-200: #c8d0e0   ← 主要文字
--white:     #f0f2f7   ← 標題/強調

功能色：
--blue-accent: #3b82f6 ← 主 accent（專業/信任）
--red-500:     #e63946 ← 威脅/危險/Critical
--amber-500:   #f59e0b ← 警告/High Risk
--green-500:   #10b981 ← 安全/系統正常
--gold:        #c9a84c ← 開發者 API 專區

風險等級色：
🔴 Critical (76-100) → red-500 / red-400
🟠 High (51-75)      → amber-500 / amber-400
🟡 Medium (26-50)    → #d4a017
🟢 Low (0-25)        → green-500 / green-400
```

#### 字體系統

```
標題/數據/導航：
  Barlow Condensed (500/600/700/800)
  - 粗體窄字型，軍事指令感
  - 全大寫 + letter-spacing: 1-2.5px
  - 用途：Logo、導航、統計數字、標題、按鈕

錢包地址/技術數據：
  IBM Plex Mono (400/500/600)
  - 專業等寬字體
  - 用途：錢包地址、API Key、交易 hash、技術數值

內文/描述：
  Barlow (400/500/600)
  - 搭配 Noto Sans TC（中文）
  - 用途：段落文字、描述、表單標籤

載入方式：Google Fonts
```

#### 視覺元素

```
頂部分類條 (Classification Bar)：
  - 紅色背景橫幅，全大寫白色文字
  - 仿機密文件分類標記
  - letter-spacing: 3px

背景紋理：
  - 60×60px 網格線（rgba(59,130,246,0.03)）
  - 模擬監控室/指揮中心螢幕感

盾牌 Logo (SVG)：
  - 雙層盾牌 + 中心圓點（監視/保護意象）
  - 藍色描邊 + 半透明填充

卡片/面板：
  - navy-900 背景 + navy-700 邊框
  - 頂部 2px 彩色指示條（依功能分色）
  - 面板標頭：深色背景 + 全大寫標題 + 圖標

風險分數 Badge：
  - 方形（40×40px, border-radius: 4px）
  - 數字居中，背景為對應風險色的 glow 版本
  - 邊框為風險色 30% 透明度

按鈕：
  - Barlow Condensed 粗體全大寫
  - letter-spacing: 1.5px
  - 主要：藍底白字 / 紅底白字
  - API：金色邊框 + 透明金色底

底部 Ops 狀態列：
  - 綠色系，顯示系統狀態/同步時間/資料筆數
  - 模擬資安營運中心 (SOC) 狀態面板
  - 綠色呼吸燈動畫
```

#### 動效

```
頁面載入：
  - 交錯淡入動畫 (staggered fadeInDown / fadeInUp)
  - 統計卡片依序進場 (0.2s → 0.5s delay)

互動回饋：
  - 搜尋框 focus 時藍色外光暈
  - 卡片 hover 微亮（blue 4% 背景）
  - 按鈕 hover 上移 1px + 色變
  - 系統狀態綠點呼吸燈 (pulse 2s infinite)

威脅條圖：
  - 寬度從 0 展開動畫 (1s ease)
```

#### 用語風格

```
英文用語偏向資安/執法術語：
  首頁       → Threat Feed
  搜尋       → Investigate
  回報       → Submit Incident Report / File a Report
  類型分佈   → Threat Classification
  最新回報   → Latest Threat Reports
  高危地址   → Highest Risk Addresses
  儀表板     → Threat Dashboard
  副標題     → Threat Intelligence Division

中文保持直觀易懂，不過度術語化
```

### 6.2 頁面路由

| 路由                       | 頁面       | 權限            |
| -------------------------- | ---------- | --------------- |
| `/`                        | 首頁       | 🔓 公開         |
| `/report`                  | 回報頁     | 🔓 公開         |
| `/search`                  | 搜尋頁     | 🔓 公開         |
| `/dashboard`               | 儀表板     | 🔓 公開         |
| `/address/:chain/:address` | 地址詳情   | 🔓 公開         |
| `/developers`              | 開發者後台 | 🔒 Passkey 登入 |
| `/developers/register`     | 開發者註冊 | 🔓 公開         |
| `/developers/login`        | 開發者登入 | 🔓 公開         |
| `/docs/api`                | API 文件   | 🔓 公開         |

### 6.3 首頁 (HomePage)

```
┌──────────────────────────────────────────────────────────────────┐
│  🛡 CryptoScamReport   [Report] [Search] [Dashboard] [API Docs] │
│                                                [Developers 🔑]   │
│                                                [EN | 中文]        │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│          「查詢錢包地址是否被回報過」                                │
│          Check if a wallet address has been reported               │
│                                                                  │
│    ┌─────────────────────────────────────────────── 🔍 ┐         │
│    │ 輸入錢包地址 Enter wallet address...               │         │
│    └───────────────────────────────────────────────────┘         │
│    [ETH] [BTC] [SOL] [TRON] [BSC] [全部]                          │
│                                                                  │
├───────────┬───────────┬───────────┬──────────────────────────────┤
│ 📊        │ 🔴        │ 💰        │  📈                          │
│ 15,234    │ 456       │ $42.5M    │  1,234                       │
│ 總回報數   │ 高危地址   │ 總損失     │  本月回報                      │
├───────────┴───────────┴───────────┴──────────────────────────────┤
│                                                                  │
│  ┌─ 我要回報詐騙 ────────────┐ ┌─ 開發者 API ─────────────────┐  │
│  │ 發現詐騙？幫助社群揭露它。  │ │ 🔑 免費 API 供錢包/DApp 整合 │  │
│  │ 無需註冊即可回報。          │ │ 每日 1,000 次免費查詢        │  │
│  │ [立即回報 →]              │ │ [取得 API Key →]             │  │
│  └───────────────────────────┘ └─────────────────────────────┘  │
│                                                                  │
│  Footer: 免責聲明 | About | API Docs | GitHub | Contact          │
└──────────────────────────────────────────────────────────────────┘
```

### 6.4 開發者後台 (DevPortalPage)

```
┌──────────────────────────────────────────────────────────────────┐
│  🔑 開發者後台 | Developer Portal           👤 CryptoHunter [登出]│
│                                                                  │
│  ┌─ API Keys ────────────────────────────────────────────────┐   │
│  │                                                           │   │
│  │  My Wallet App                              [建立新 Key +] │   │
│  │  ┌─────────────────────────────────────────────────────┐  │   │
│  │  │ Key:     csr_7f3a9b2c••••••••                       │  │   │
│  │  │ 狀態:    🟢 啟用中                                    │  │   │
│  │  │ 今日用量: ████░░░░░░  156 / 1,000                    │  │   │
│  │  │ 累計:    12,450 次                                   │  │   │
│  │  │ 最後使用: 30 分鐘前                                   │  │   │
│  │  │                        [停用] [刪除]                  │  │   │
│  │  └─────────────────────────────────────────────────────┘  │   │
│  │                                                           │   │
│  │  Trading Bot                                              │   │
│  │  ┌─────────────────────────────────────────────────────┐  │   │
│  │  │ Key:     csr_4e2d8a1f••••••••                       │  │   │
│  │  │ 狀態:    🟢 啟用中                                    │  │   │
│  │  │ 今日用量: █░░░░░░░░░  23 / 1,000                     │  │   │
│  │  │ 累計:    890 次                                      │  │   │
│  │  │                        [停用] [刪除]                  │  │   │
│  │  └─────────────────────────────────────────────────────┘  │   │
│  │                                                           │   │
│  │  帳號限制：最多 5 個 API Key                               │   │
│  └───────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌─ 快速開始 ────────────────────────────────────────────────┐   │
│  │                                                           │   │
│  │  curl -H "X-API-Key: YOUR_KEY" \                          │   │
│  │    https://cryptoscamreport.com/v1/search?q=0xAbC123      │   │
│  │                                                           │   │
│  │  [查看完整 API 文件 →]                                     │   │
│  └───────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌─ Passkey 管理 ────────────────────────────────────────────┐   │
│  │  已註冊的 Passkey：                                        │   │
│  │  • MacBook Pro (Touch ID)     註冊於 2025-01-01            │   │
│  │  • iPhone 15 (Face ID)        註冊於 2025-01-05            │   │
│  │                               [新增 Passkey +]             │   │
│  └───────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
```

### 6.5 開發者註冊頁

```
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│                  🔑 建立開發者帳號                                 │
│                  Create Developer Account                        │
│                                                                  │
│  ┌───────────────────────────────────────────────────────────┐   │
│  │                                                           │   │
│  │  顯示名稱 Display Name（選填）                              │   │
│  │  ┌─────────────────────────────────────────────────────┐  │   │
│  │  │ CryptoHunter                                        │  │   │
│  │  └─────────────────────────────────────────────────────┘  │   │
│  │  不填寫將自動生成匿名代號                                    │   │
│  │                                                           │   │
│  │  [✓] Cloudflare Turnstile                                 │   │
│  │                                                           │   │
│  │  ┌─────────────────────────────────────────────────────┐  │   │
│  │  │  🔑  使用 Passkey 註冊                                │  │   │
│  │  │  點擊後啟動生物辨識驗證                                 │  │   │
│  │  │  (Face ID / Touch ID / Windows Hello / PIN)         │  │   │
│  │  │              [ 建立 Passkey → ]                      │  │   │
│  │  └─────────────────────────────────────────────────────┘  │   │
│  │                                                           │   │
│  │  ℹ️ 註冊後即可在開發者後台建立 API Key                       │   │
│  │  已有帳號？ [登入 →]                                       │   │
│  └───────────────────────────────────────────────────────────┘   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 6.6 回報表單 (ReportPage)

```
┌──────────────────────────────────────────────────────────────────┐
│  回報詐騙/駭客事件 | Report Scam/Hack Incident                    │
│                                                                  │
│  Step 1: 基本資訊                                                 │
│  ┌───────────────────────────────────────────────────────────┐   │
│  │ 區塊鏈 *    [▼ ETH / BTC / SOL / TRON / BSC / ...]        │   │
│  │ 詐騙地址 *  [                                          ]   │   │
│  │             ✅ 有效的 ETH 地址                               │   │
│  │ 詐騙類型 *  [▼ 釣魚 / Rug Pull / 假交易所 / ...]           │   │
│  └───────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Step 2: 事件詳情（選填）                                          │
│  ┌───────────────────────────────────────────────────────────┐   │
│  │ 事件描述    [                              0/2000字     ]   │   │
│  │ 損失金額    [           ] [▼ USD / USDT / ETH / BTC    ]   │   │
│  │ 證據連結    [https://...                                ]   │   │
│  │ 相關交易    [0x...                                      ]   │   │
│  └───────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Step 3: 驗證                                                     │
│  ┌───────────────────────────────────────────────────────────┐   │
│  │  [✓] Cloudflare Turnstile 人機驗證                         │   │
│  └───────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ⚠️ 系統將記錄您的 IP 地址，以防止濫用。                             │
│                                                                  │
│                              [提交回報 Submit Report →]           │
└──────────────────────────────────────────────────────────────────┘
```

### 6.7 搜尋頁 (SearchPage) — 完全公開

```
┌──────────────────────────────────────────────────────────────────┐
│  搜尋詐騙地址 | Search Scam Addresses                             │
│                                                                  │
│  ┌────────────────────────────────────────────────── 🔍 ┐        │
│  │ 輸入錢包地址或關鍵字...                                  │        │
│  └──────────────────────────────────────────────────────┘        │
│                                                                  │
│  篩選: [鏈別 ▼] [詐騙類型 ▼] [日期範圍 ▼] [排序: 最新 ▼]          │
│                                                                  │
│  找到 156 筆結果                                                  │
│  ┌───────────────────────────────────────────────────────────┐   │
│  │ 🔴 92  0xA1b2C3d4...E5f6G7h8                             │   │
│  │        ETH | Rug Pull, Phishing | $85,000 損失             │   │
│  │        12 筆回報 | 最後回報: 2 小時前                        │   │
│  ├───────────────────────────────────────────────────────────┤   │
│  │ 🟠 68  bc1qxy2kgdygjrs...7890                             │   │
│  │        BTC | Phishing | $12,500 損失                       │   │
│  │        5 筆回報 | 最後回報: 1 天前                           │   │
│  └───────────────────────────────────────────────────────────┘   │
│                                                                  │
│  [← 上一頁]  第 1 頁 / 共 8 頁  [下一頁 →]                        │
└──────────────────────────────────────────────────────────────────┘
```

### 6.8 API 文件頁 (ApiDocsPage)

```
┌──────────────────────────────────────────────────────────────────┐
│  📖 API Documentation                                            │
│                                                                  │
│  ┌─ 快速開始 ────────────────────────────────────────────────┐   │
│  │ 1. 建立開發者帳號         [前往註冊 →]                      │   │
│  │ 2. 在開發者後台建立 API Key                                │   │
│  │ 3. 在請求中加入 X-API-Key Header                          │   │
│  └───────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Base URL: https://cryptoscamreport.com/v1                       │
│                                                                  │
│  認證方式:                                                        │
│  ┌───────────────────────────────────────────────────────────┐   │
│  │ X-API-Key: csr_your_api_key_here                          │   │
│  └───────────────────────────────────────────────────────────┘   │
│                                                                  │
│  Rate Limit: 1,000 requests/day (Free)                           │
│                                                                  │
│  [Endpoints]                                                     │
│  ├── GET  /v1/search              搜尋地址                       │
│  ├── GET  /v1/address/:chain/:addr 地址風險詳情                   │
│  ├── POST /v1/reports             提交回報                       │
│  ├── POST /v1/batch/addresses     批量查詢（max 50）              │
│  ├── GET  /v1/stats/overview      統計總覽                       │
│  ├── GET  /v1/stats/trends        趨勢數據                       │
│  └── GET  /v1/stats/breakdown     分佈數據                       │
│                                                                  │
│  [各端點詳細文件、請求/回應範例...]                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 7. 安全設計 | Security Design

### 7.1 安全架構

```
┌──────────────────────────────────────────────────────────────┐
│                     請求來源                                  │
├──────────────────────┬───────────────────────────────────────┤
│ 🌐 網站前端           │ 🔑 Developer API                      │
│ /api/*               │ /v1/*                                │
├──────────────────────┼───────────────────────────────────────┤
│                      │                                      │
│ Cloudflare Edge      │ Cloudflare Edge                      │
│ (DDoS + WAF + TLS)   │ (DDoS + WAF + TLS)                   │
│        │             │        │                             │
│        ▼             │        ▼                             │
│ CORS 驗證             │ API Key 驗證                          │
│ (僅允許前端 origin)    │ (X-API-Key Header)                   │
│        │             │        │                             │
│        ▼             │        ▼                             │
│ Turnstile (POST)     │ Key Hash 比對 (D1)                   │
│        │             │ is_active 檢查                       │
│        ▼             │        │                             │
│ IP Rate Limit        │        ▼                             │
│ (KV-based)           │ Daily Limit 檢查                     │
│        │             │ (daily_usage < daily_limit)          │
│        ▼             │        │                             │
│ Input Sanitize       │        ▼                             │
│        │             │ Input Sanitize                       │
│        ▼             │        │                             │
│ Business Logic       │        ▼                             │
│ (IP 記錄)             │ Business Logic                       │
│        │             │ (API Key ID 記錄)                    │
│        ▼             │        ▼                             │
│ D1 Database          │ D1 Database                          │
│ (參數化查詢)          │ (參數化查詢)                          │
└──────────────────────┴───────────────────────────────────────┘
```

### 7.2 IP 記錄策略

```
回報時記錄：
┌────────────────────────────────────────────────────┐
│ reporter_ip      │ "203.0.113.45"                  │
│ reporter_ip_hash │ SHA-256(IP + daily_salt)         │
│ reporter_ua      │ "Mozilla/5.0..."                │
│ source           │ "web" 或 "api"                   │
│ api_key_id       │ (API 來源時記錄)                  │
└────────────────────────────────────────────────────┘

IP 來源：request.headers.get('CF-Connecting-IP')
```

### 7.3 速率限制

#### 網站前端 (IP-based)

| 路由                 | 限制          |
| -------------------- | ------------- |
| `POST /api/reports`  | 10 次/IP/小時 |
| `GET /api/search`    | 60 次/IP/分鐘 |
| `GET /api/address/*` | 60 次/IP/分鐘 |
| `GET /api/stats/*`   | 30 次/IP/分鐘 |

#### Developer API (API Key-based)

| 方案          | 每日限額       | 批量查詢           | 費用  |
| ------------- | -------------- | ------------------ | ----- |
| Free          | 1,000 次/天    | 最多 50 地址/次    | 免費  |
| _（未來）Pro_ | _10,000 次/天_ | _最多 200 地址/次_ | _TBD_ |

#### 開發者後台 (Session-based)

| 路由                        | 限制          |
| --------------------------- | ------------- |
| `POST /api/auth/register/*` | 5 次/IP/小時  |
| `POST /api/auth/login/*`    | 10 次/IP/小時 |
| `POST /api/keys`            | 10 次/User/天 |

### 7.4 API Key 安全

| 措施           | 說明                                 |
| -------------- | ------------------------------------ |
| **Hash 儲存**  | 只存 SHA-256(key)，原始 Key 不落地   |
| **僅顯示一次** | 建立時完整顯示，之後只顯示前 12 字元 |
| **即時停用**   | 開發者可隨時停用/刪除 Key            |
| **用量追蹤**   | 每次請求計數，超額則拒絕             |
| **來源追蹤**   | 每筆 API 回報記錄 api_key_id         |
| **數量限制**   | 每帳號最多 5 個 Key                  |

### 7.5 HTTP 安全標頭

```
Content-Security-Policy: default-src 'self'; script-src 'self' https://challenges.cloudflare.com
X-Content-Type-Options: nosniff
X-Frame-Options: DENY
Referrer-Policy: strict-origin-when-cross-origin
Permissions-Policy: camera=(), microphone=(), geolocation=()
Strict-Transport-Security: max-age=31536000; includeSubDomains
```

---

## 8. 支援的區塊鏈與詐騙類型

### 8.1 區塊鏈

| Chain ID | 名稱      | 地址格式           | 驗證規則             |
| -------- | --------- | ------------------ | -------------------- |
| `ETH`    | Ethereum  | `0x` + 40 hex      | EIP-55 checksum      |
| `BTC`    | Bitcoin   | `1`/`3`/`bc1` 開頭 | Base58Check / Bech32 |
| `SOL`    | Solana    | 32-44 字元 Base58  | Base58 decode        |
| `TRON`   | TRON      | `T` 開頭 34 字元   | Base58Check          |
| `BSC`    | BNB Chain | `0x` + 40 hex      | 同 ETH               |
| `MATIC`  | Polygon   | `0x` + 40 hex      | 同 ETH               |
| `OTHER`  | 其他鏈    | 自由格式           | 長度 ≤ 128           |

### 8.2 詐騙類型

| Type ID         | 中文     | English       |
| --------------- | -------- | ------------- |
| `phishing`      | 釣魚詐騙 | Phishing      |
| `rug_pull`      | 捲款跑路 | Rug Pull      |
| `fake_exchange` | 假交易所 | Fake Exchange |
| `hack`          | 駭客攻擊 | Hack/Exploit  |
| `ponzi`         | 龐氏騙局 | Ponzi Scheme  |
| `impersonation` | 冒充詐騙 | Impersonation |
| `fake_airdrop`  | 假空投   | Fake Airdrop  |
| `romance`       | 感情詐騙 | Romance Scam  |
| `other`         | 其他     | Other         |

---

## 9. 國際化 | i18n

### 語言偵測

```
1. URL 參數 ?lang=en
2. localStorage 偏好
3. navigator.language
4. 預設 zh-TW
```

---

## 10. 效能與快取

| 資料類型           | 快取位置 | TTL     |
| ------------------ | -------- | ------- |
| 統計總覽           | KV       | 5 分鐘  |
| 趨勢/分佈          | KV       | 15 分鐘 |
| 地址詳情           | KV       | 2 分鐘  |
| Session            | KV       | 7 天    |
| WebAuthn Challenge | KV       | 60 秒   |
| API Key 驗證結果   | KV       | 5 分鐘  |
| 靜態資源           | CDN      | 1 年    |

---

## 11. 部署

### wrangler.toml

```toml
name = "cryptoscam-api"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[[d1_databases]]
binding = "DB"
database_name = "cryptoscam-db"
database_id = "<YOUR_D1_ID>"

[[kv_namespaces]]
binding = "CACHE"
id = "<YOUR_KV_ID>"

[vars]
TURNSTILE_SECRET = ""
CORS_ORIGIN = "https://cryptoscamreport.pages.dev"
WEBAUTHN_RP_NAME = "CryptoScamReport"
WEBAUTHN_RP_ID = "cryptoscamreport.pages.dev"
WEBAUTHN_ORIGIN = "https://cryptoscamreport.pages.dev"
```

### Cloudflare Free Tier

| 資源         | 免費額度   | 預估用量    |
| ------------ | ---------- | ----------- |
| Workers 請求 | 100,000/天 | < 10,000/天 |
| D1 讀取      | 5M/天      | < 100K/天   |
| D1 寫入      | 100K/天    | < 5K/天     |
| D1 儲存      | 5GB        | < 100MB     |
| KV 讀取      | 100K/天    | < 50K/天    |
| KV 寫入      | 1K/天      | < 500/天    |
| Turnstile    | 無限       | ✓           |

---

## 12. CI/CD 與程式碼品質 | CI/CD & Code Quality

### 12.1 Linting & Formatting 工具

| 工具                    | 用途                        | 適用範圍          |
| ----------------------- | --------------------------- | ----------------- |
| **ESLint**              | TypeScript / React 靜態分析 | frontend + worker |
| **Prettier**            | 程式碼格式化                | 全專案            |
| **TypeScript Compiler** | 型別檢查（`tsc --noEmit`）  | frontend + worker |

#### ESLint 設定策略

```
前後端共用規則：
  - @typescript-eslint/recommended
  - @typescript-eslint/strict-type-checked
  - no-unused-vars (error)
  - no-explicit-any (warn)
  - no-console (warn, 允許 console.error)
  - eqeqeq (error)
  - prefer-const (error)

前端額外規則：
  - eslint-plugin-react-hooks/recommended
  - eslint-plugin-react-refresh
  - react/jsx-no-target-blank (error)

配置檔位置：
  /frontend/eslint.config.ts    ← Flat config (ESLint v9)
  /worker/eslint.config.ts      ← Flat config (ESLint v9)
  /prettier.config.js           ← 根目錄共用
```

#### Prettier 設定

```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "all",
  "tabWidth": 2,
  "printWidth": 100,
  "arrowParens": "always",
  "endOfLine": "lf"
}
```

#### package.json Scripts

```json
// 根目錄 package.json (monorepo scripts)
{
  "scripts": {
    "lint": "npm run lint:frontend && npm run lint:worker",
    "lint:frontend": "cd frontend && eslint src/ --max-warnings 0",
    "lint:worker": "cd worker && eslint src/ --max-warnings 0",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,json,md}\"",
    "typecheck": "npm run typecheck:frontend && npm run typecheck:worker",
    "typecheck:frontend": "cd frontend && tsc --noEmit",
    "typecheck:worker": "cd worker && tsc --noEmit",
    "build:frontend": "cd frontend && npm run build",
    "build:worker": "cd worker && npm run build",
    "test": "npm run test:frontend && npm run test:worker",
    "test:frontend": "cd frontend && vitest run",
    "test:worker": "cd worker && vitest run",
    "ci": "npm run lint && npm run format:check && npm run typecheck && npm run test && npm run build:frontend && npm run build:worker"
  }
}
```

### 12.2 GitHub Actions Workflows

#### 分支策略

```
main          ← 正式環境（自動部署到 Cloudflare Production）
├── develop   ← 開發環境（自動部署到 Cloudflare Preview）
│   ├── feature/*   ← 功能分支（PR → develop）
│   ├── fix/*       ← 修復分支（PR → develop）
│   └── refactor/*  ← 重構分支（PR → develop）
└── release/* ← 發版分支（PR → main）
```

#### Workflow 1：CI — Pull Request 檢查

```yaml
# .github/workflows/ci.yml
name: CI

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint & Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm ci
          cd frontend && npm ci
          cd ../worker && npm ci

      - name: ESLint (Frontend)
        run: npm run lint:frontend

      - name: ESLint (Worker)
        run: npm run lint:worker

      - name: Prettier Check
        run: npm run format:check

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm ci
          cd frontend && npm ci
          cd ../worker && npm ci

      - name: TypeScript Check (Frontend)
        run: npm run typecheck:frontend

      - name: TypeScript Check (Worker)
        run: npm run typecheck:worker

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: typecheck
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm ci
          cd frontend && npm ci
          cd ../worker && npm ci

      - name: Test Frontend
        run: npm run test:frontend

      - name: Test Worker
        run: npm run test:worker

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm ci
          cd frontend && npm ci
          cd ../worker && npm ci

      - name: Build Frontend
        run: npm run build:frontend

      - name: Build Worker
        run: npm run build:worker

      - name: Upload frontend build artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist/
          retention-days: 3
```

#### Workflow 2：CD — 部署到 Cloudflare

```yaml
# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches: [main, develop]

concurrency:
  group: deploy-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  ci:
    name: CI Checks
    uses: ./.github/workflows/ci.yml

  deploy-worker:
    name: Deploy Worker
    runs-on: ubuntu-latest
    needs: ci
    environment:
      name: ${{ github.ref_name == 'main' && 'production' || 'preview' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: cd worker && npm ci

      - name: Run D1 Migrations
        if: github.ref_name == 'main'
        run: cd worker && npx wrangler d1 migrations apply cryptoscam-db --remote
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Deploy Worker (Production)
        if: github.ref_name == 'main'
        run: cd worker && npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Deploy Worker (Preview)
        if: github.ref_name == 'develop'
        run: cd worker && npx wrangler deploy --env preview
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

  deploy-frontend:
    name: Deploy Frontend (Pages)
    runs-on: ubuntu-latest
    needs: ci
    environment:
      name: ${{ github.ref_name == 'main' && 'production' || 'preview' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install & Build
        run: |
          cd frontend && npm ci
          npm run build

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy frontend/dist --project-name=cryptoscamreport --branch=${{ github.ref_name }}
```

### 12.3 CI Pipeline 流程圖

```
Pull Request 建立 / Push to develop
    │
    ▼
┌─────────────────┐
│   Lint & Format  │  ← ESLint (frontend + worker) + Prettier check
│   (~30s)         │  ← 任一失敗即中斷
└────────┬────────┘
         │ ✅ Pass
         ▼
┌─────────────────┐
│  Type Check      │  ← tsc --noEmit (frontend + worker)
│  (~20s)          │  ← 型別錯誤即中斷
└────────┬────────┘
         │ ✅ Pass
         ▼
┌─────────────────┐
│  Unit Tests      │  ← Vitest (frontend + worker)
│  (~40s)          │
└────────┬────────┘
         │ ✅ Pass
         ▼
┌─────────────────┐
│  Build           │  ← Vite build (frontend) + Worker build
│  (~30s)          │  ← 建置失敗即中斷
└────────┬────────┘
         │ ✅ All Pass
         ▼
┌─────────────────────────────────────┐
│  PR 可合併 ✅                        │
│  Push to develop → 自動部署 Preview   │
│  Push to main    → 自動部署 Production│
└─────────────────────────────────────┘
```

### 12.4 GitHub Secrets 設定

| Secret 名稱             | 說明                 | 取得方式                                                                       |
| ----------------------- | -------------------- | ------------------------------------------------------------------------------ |
| `CLOUDFLARE_API_TOKEN`  | Cloudflare API Token | Cloudflare Dashboard → API Tokens → 建立 Token（需 Workers / Pages / D1 權限） |
| `CLOUDFLARE_ACCOUNT_ID` | Cloudflare 帳號 ID   | Cloudflare Dashboard → 右側欄 Account ID                                       |

### 12.5 Branch Protection Rules（建議）

```
main 分支保護：
  ✅ Require pull request before merging
  ✅ Require status checks to pass (lint, typecheck, test, build)
  ✅ Require branches to be up to date
  ✅ Require linear history (squash merge)
  ❌ Allow force pushes

develop 分支保護：
  ✅ Require status checks to pass (lint, typecheck, test, build)
  ✅ Require branches to be up to date
```

### 12.6 Commit Convention

```
採用 Conventional Commits：

feat:     新功能                feat: add batch address query endpoint
fix:      修復                  fix: address validation for TRON chain
refactor: 重構                  refactor: extract risk score calculation
style:    格式/樣式（不影響邏輯） style: fix eslint warnings in search page
docs:     文件                  docs: update API documentation
test:     測試                  test: add unit tests for auth service
chore:    維護/工具              chore: update dependencies
ci:       CI/CD 變更            ci: add deploy preview workflow
```

### 12.7 Pre-commit Hook（選用）

```json
// package.json — 使用 husky + lint-staged
{
  "devDependencies": {
    "husky": "^9.x",
    "lint-staged": "^15.x"
  },
  "lint-staged": {
    "*.{ts,tsx}": ["eslint --fix --max-warnings 0", "prettier --write"],
    "*.{json,md}": ["prettier --write"]
  }
}
```

```bash
# 安裝 husky
npx husky init
echo "npx lint-staged" > .husky/pre-commit
```

---

## 13. SEO 策略 | Search Engine Optimization

### 13.1 SEO 架構挑戰與解法

本專案使用 React SPA，對 SEO 天生不利。需採取以下對策：

```
問題：React SPA 預設為 Client-Side Rendering (CSR)
     → 搜尋引擎爬蟲看到空白 HTML → 無法索引內容

解法：

方案 A（推薦）：Prerendering
  ┌─────────────────────────────────────────────────┐
  │  Vite + vite-plugin-ssr / prerender             │
  │  建置時產生靜態 HTML → 部署到 Cloudflare Pages    │
  │                                                 │
  │  首頁、搜尋頁、儀表板、API 文件 → 靜態 HTML       │
  │  地址詳情頁 → Workers SSR or ISR                 │
  └─────────────────────────────────────────────────┘

方案 B：Workers SSR (Server-Side Rendering)
  ┌─────────────────────────────────────────────────┐
  │  Cloudflare Workers 攔截爬蟲請求                  │
  │  User-Agent 偵測 Googlebot/Bingbot              │
  │  → 回傳完整渲染的 HTML                           │
  │  → 一般使用者仍走 SPA                            │
  └─────────────────────────────────────────────────┘

最終建議：混合策略
  - 靜態頁面（首頁/文件/儀表板）→ 建置時 Prerender
  - 動態頁面（地址詳情/搜尋結果）→ Workers SSR
```

### 13.2 Meta Tags 規範

#### 全域 HTML Head

```html
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- 基本 SEO -->
  <title>{page_title} | CryptoScamReport — Crypto Threat Intelligence</title>
  <meta name="description" content="{page_description}">
  <meta name="keywords" content="crypto scam, blockchain fraud, wallet check, scam report, 虛擬貨幣詐騙, 錢包查詢">
  <meta name="author" content="CryptoScamReport">
  <link rel="canonical" href="https://cryptoscamreport.com{path}">

  <!-- 語言 -->
  <html lang="zh-TW">
  <link rel="alternate" hreflang="zh-TW" href="https://cryptoscamreport.com{path}?lang=zh-TW">
  <link rel="alternate" hreflang="en" href="https://cryptoscamreport.com{path}?lang=en">
  <link rel="alternate" hreflang="x-default" href="https://cryptoscamreport.com{path}">

  <!-- Open Graph (Facebook / LINE / Telegram) -->
  <meta property="og:type" content="website">
  <meta property="og:site_name" content="CryptoScamReport">
  <meta property="og:title" content="{og_title}">
  <meta property="og:description" content="{og_description}">
  <meta property="og:image" content="https://cryptoscamreport.com/og-image.png">
  <meta property="og:url" content="https://cryptoscamreport.com{path}">
  <meta property="og:locale" content="zh_TW">
  <meta property="og:locale:alternate" content="en_US">

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="{twitter_title}">
  <meta name="twitter:description" content="{twitter_description}">
  <meta name="twitter:image" content="https://cryptoscamreport.com/og-image.png">

  <!-- Robots -->
  <meta name="robots" content="{robots}">

  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="/favicon.svg">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <meta name="theme-color" content="#0a0e1a">
</head>
```

#### 各頁面 Meta 配置

| 頁面                             | title                                                 | description                                                    | robots              |
| -------------------------------- | ----------------------------------------------------- | -------------------------------------------------------------- | ------------------- |
| 首頁 `/`                         | CryptoScamReport — Crypto Threat Intelligence         | 免費查詢錢包地址是否涉及詐騙。社群驅動的加密貨幣威脅情報平台。 | `index, follow`     |
| 回報 `/report`                   | 回報詐騙事件 — CryptoScamReport                       | 匿名回報虛擬貨幣詐騙與駭客攻擊事件，協助社群防範加密犯罪。     | `index, follow`     |
| 搜尋 `/search`                   | 搜尋詐騙地址 — CryptoScamReport                       | 搜尋已知的詐騙錢包地址，查看風險評分與社群回報紀錄。           | `index, follow`     |
| 儀表板 `/dashboard`              | 威脅數據儀表板 — CryptoScamReport                     | 即時加密貨幣詐騙統計數據、趨勢分析與威脅分佈圖表。             | `index, follow`     |
| 地址詳情 `/address/:chain/:addr` | {chain} 地址 {addr_short} 風險報告 — CryptoScamReport | 地址 {addr_short} 的風險評分為 {score}，共 {count} 筆回報。    | `index, follow`     |
| API 文件 `/docs/api`             | Developer API Documentation — CryptoScamReport        | 免費 REST API，為錢包與 DApp 整合即時加密貨幣風險查詢。        | `index, follow`     |
| 開發者後台 `/developers`         | 開發者後台 — CryptoScamReport                         | 管理 API Key 與查看用量統計。                                  | `noindex, nofollow` |
| 註冊/登入 `/developers/register` | 建立開發者帳號 — CryptoScamReport                     | 免費註冊取得 API Key，整合加密貨幣風險查詢 API。               | `index, follow`     |

### 13.3 結構化資料 (JSON-LD)

#### 首頁 — WebSite + SearchAction

```json
{
  "@context": "https://schema.org",
  "@type": "WebSite",
  "name": "CryptoScamReport",
  "alternateName": "加密貨幣詐騙回報平台",
  "url": "https://cryptoscamreport.com",
  "description": "Community-powered cryptocurrency scam and hack incident reporting platform",
  "potentialAction": {
    "@type": "SearchAction",
    "target": {
      "@type": "EntryPoint",
      "urlTemplate": "https://cryptoscamreport.com/search?q={search_term_string}"
    },
    "query-input": "required name=search_term_string"
  }
}
```

> `SearchAction` 讓 Google 可能在搜尋結果直接顯示站內搜尋框。

#### 首頁 — Organization

```json
{
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "CryptoScamReport",
  "url": "https://cryptoscamreport.com",
  "logo": "https://cryptoscamreport.com/logo.png",
  "description": "Free, community-powered crypto threat intelligence platform",
  "sameAs": ["https://twitter.com/cryptoscamreport", "https://github.com/cryptoscamreport"]
}
```

#### 地址詳情頁 — 自訂結構化資料

```json
{
  "@context": "https://schema.org",
  "@type": "WebPage",
  "name": "ETH Address 0xA1b2...E5f6 Threat Report",
  "description": "Risk score 92/100 (Critical). 12 community reports. Total estimated loss: $85,000.",
  "dateModified": "2025-01-15T08:30:00Z",
  "mainEntity": {
    "@type": "Thing",
    "name": "0xA1b2C3d4E5f6G7h8",
    "description": "Ethereum address flagged for Rug Pull and Phishing with 12 reports"
  }
}
```

#### 儀表板 — Dataset

```json
{
  "@context": "https://schema.org",
  "@type": "Dataset",
  "name": "CryptoScamReport Threat Database",
  "description": "Community-reported cryptocurrency scam and hack incidents database",
  "url": "https://cryptoscamreport.com/dashboard",
  "license": "https://cryptoscamreport.com/terms",
  "temporalCoverage": "2024/..",
  "variableMeasured": [
    "Scam incident count",
    "Estimated financial loss (USD)",
    "Risk score per address"
  ]
}
```

### 13.4 Sitemap 與 Robots.txt

#### robots.txt

```txt
User-agent: *
Allow: /
Disallow: /developers
Disallow: /api/
Disallow: /v1/

Sitemap: https://cryptoscamreport.com/sitemap.xml
```

#### sitemap.xml（靜態 + 動態生成）

```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <!-- 靜態頁面 -->
  <url>
    <loc>https://cryptoscamreport.com/</loc>
    <changefreq>daily</changefreq>
    <priority>1.0</priority>
  </url>
  <url>
    <loc>https://cryptoscamreport.com/report</loc>
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
  </url>
  <url>
    <loc>https://cryptoscamreport.com/search</loc>
    <changefreq>daily</changefreq>
    <priority>0.9</priority>
  </url>
  <url>
    <loc>https://cryptoscamreport.com/dashboard</loc>
    <changefreq>daily</changefreq>
    <priority>0.8</priority>
  </url>
  <url>
    <loc>https://cryptoscamreport.com/docs/api</loc>
    <changefreq>weekly</changefreq>
    <priority>0.7</priority>
  </url>

  <!-- 動態頁面：高風險地址（risk_score >= 50） -->
  <!-- 由 Workers Cron 每日生成 -->
  <url>
    <loc>https://cryptoscamreport.com/address/ETH/0xA1b2C3d4E5f6</loc>
    <lastmod>2025-01-15</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.6</priority>
  </url>
  <!-- ... 更多動態地址頁 ... -->
</urlset>
```

#### 動態 Sitemap 生成（Workers Cron）

```
策略：
  - 每日凌晨由 Scheduled Worker 自動生成
  - 僅納入 risk_score >= 50 的地址（避免 Sitemap 過大）
  - 存放至 KV 或 R2，由 Pages 路由回傳
  - 上限 50,000 URLs（Google 單一 Sitemap 限制）
  - 超過時拆分為 sitemap-index.xml + 多個 sitemap-N.xml
```

### 13.5 URL 結構設計

```
目標：乾淨、語意化、對搜尋引擎友善

✅ 良好 URL：
  /                                    ← 首頁
  /report                              ← 回報
  /search                              ← 搜尋
  /search?q=0xAbC&chain=ETH            ← 搜尋結果（帶查詢參數）
  /dashboard                           ← 儀表板
  /address/ETH/0xA1b2C3d4E5f6G7h8     ← 地址詳情
  /address/BTC/bc1qxy2kgdygjrs7890     ← 地址詳情
  /docs/api                            ← API 文件

❌ 避免：
  /address?chain=ETH&addr=0xABC        ← 不要把地址放在 query string
  /#/search                            ← 不要用 hash routing
  /page/123                            ← 不要用無語意 ID
```

### 13.6 Core Web Vitals 目標

| 指標                                | 目標    | 說明                               |
| ----------------------------------- | ------- | ---------------------------------- |
| **LCP** (Largest Contentful Paint)  | < 1.5s  | 首屏最大元素（搜尋框區塊）載入時間 |
| **INP** (Interaction to Next Paint) | < 100ms | 使用者互動回應速度                 |
| **CLS** (Cumulative Layout Shift)   | < 0.05  | 頁面穩定度，避免跳動               |
| **FCP** (First Contentful Paint)    | < 1.0s  | 第一個內容出現的時間               |
| **TTFB** (Time to First Byte)       | < 100ms | Cloudflare 邊緣節點回應            |

#### 最佳化手段

```
LCP 最佳化：
  - Prerender 首頁 HTML（不等待 JS）
  - 關鍵 CSS 內聯 (Critical CSS inlining)
  - 字體 preload：<link rel="preload" href="fonts/..." as="font">
  - 統計數據 KV 快取（避免首屏等待 API）

INP 最佳化：
  - 搜尋輸入 debounce 300ms
  - React.memo 避免不必要重渲染
  - 圖表元件 lazy load

CLS 最佳化：
  - 統計卡片設定固定高度 / aspect-ratio
  - 字體設定 font-display: swap + size-adjust
  - 圖片/圖表設定明確寬高

Bundle 最佳化：
  - Route-based code splitting (React.lazy)
  - 圖表庫（Recharts）僅在 /dashboard 載入
  - Tree shaking + minification (Vite 預設)
  - 目標 < 180KB gzipped（首次載入）
```

### 13.7 多語系 SEO (hreflang)

```
策略：URL 參數切換語言，hreflang 標籤告知搜尋引擎

每個頁面的 <head> 包含：
  <link rel="alternate" hreflang="zh-TW" href="https://cryptoscamreport.com/search?lang=zh-TW">
  <link rel="alternate" hreflang="en"    href="https://cryptoscamreport.com/search?lang=en">
  <link rel="alternate" hreflang="x-default" href="https://cryptoscamreport.com/search">

Google 會根據使用者語言偏好顯示對應版本。
x-default 為未指定語言時的預設。
```

### 13.8 社群分享最佳化 (OG Image)

```
og-image.png 規格：
  尺寸：1200 × 630 px（Facebook / LINE / Telegram 最佳）
  內容：
    - 盾牌 Logo + "CryptoScamReport" 品牌名
    - 副標題 "Crypto Threat Intelligence Platform"
    - 深海軍藍背景 + 網格紋理（與網站一致）

地址詳情頁動態 OG Image（進階）：
  /api/og/address/ETH/0xAbC123
  → Workers 動態生成 PNG：
    - 地址 + 風險分數 + 風險等級色塊
    - 回報次數 + 損失金額
  → 分享到社群媒體時顯示具體風險資訊
  → 使用 @vercel/og 或 Satori 在 Workers 端生成

Twitter / LINE 分享效果：
  ┌──────────────────────────────────────┐
  │  🛡 CryptoScamReport                 │
  │  ──────────────────────────────────  │
  │  ETH 0xA1b2...E5f6                  │
  │  ██████████ Risk Score: 92 🔴        │
  │  12 Reports | $85,000 Lost           │
  └──────────────────────────────────────┘
```

### 13.9 SEO 監測工具

| 工具                         | 用途                                | 費用            |
| ---------------------------- | ----------------------------------- | --------------- |
| **Google Search Console**    | 索引狀態、搜尋表現、Core Web Vitals | 免費            |
| **Bing Webmaster Tools**     | Bing 索引管理                       | 免費            |
| **Cloudflare Web Analytics** | 隱私友善的流量分析                  | 免費            |
| **Google Lighthouse**        | 頁面效能/SEO/無障礙評分             | 免費（CI 整合） |

#### Lighthouse CI 整合（選用）

```yaml
# 可加入 .github/workflows/ci.yml
- name: Lighthouse CI
  uses: treosh/lighthouse-ci-action@v11
  with:
    urls: |
      https://cryptoscamreport.com/
      https://cryptoscamreport.com/search
      https://cryptoscamreport.com/dashboard
    budgetPath: .github/lighthouse-budget.json
```

```json
// .github/lighthouse-budget.json
[
  {
    "path": "/*",
    "timings": [
      { "metric": "largest-contentful-paint", "budget": 1500 },
      { "metric": "first-contentful-paint", "budget": 1000 },
      { "metric": "cumulative-layout-shift", "budget": 0.05 }
    ],
    "resourceSizes": [
      { "resourceType": "script", "budget": 200 },
      { "resourceType": "total", "budget": 500 }
    ]
  }
]
```

### 13.10 SEO 檢查清單

| 項目                             | 狀態      | 說明                                            |
| -------------------------------- | --------- | ----------------------------------------------- |
| Prerender / SSR                  | 📋 待實作 | 靜態頁 Prerender + 動態頁 Workers SSR           |
| Meta tags（title / description） | 📋 待實作 | 每頁獨立配置                                    |
| Open Graph tags                  | 📋 待實作 | 含動態 OG Image                                 |
| JSON-LD 結構化資料               | 📋 待實作 | WebSite + Organization + SearchAction + Dataset |
| hreflang 多語系標籤              | 📋 待實作 | zh-TW / en / x-default                          |
| Canonical URL                    | 📋 待實作 | 每頁唯一 canonical                              |
| robots.txt                       | 📋 待實作 | 封鎖 /developers 與 API 路由                    |
| sitemap.xml                      | 📋 待實作 | 靜態 + 動態（每日 Cron 生成）                   |
| Core Web Vitals                  | 📋 待驗證 | LCP < 1.5s, INP < 100ms, CLS < 0.05             |
| 語意化 URL 結構                  | ✅ 已設計 | /address/:chain/:address                        |
| Google Search Console            | 📋 待設定 | 上線後提交                                      |
| Lighthouse CI                    | 📋 選用   | 可加入 GitHub Actions                           |

---

## 14. 隱私權政策要點

```
我們收集的資訊：

1. 回報時：
   - 您的 IP 地址（防濫用及配合執法需求）
   - 瀏覽器 User-Agent
   - 回報內容

2. 開發者註冊時：
   - 顯示名稱（或自動生成匿名代號）
   - Passkey 公鑰（私鑰永遠不離開您的裝置）

3. 我們不收集：
   - Email / 手機 / 真實姓名

IP 保留期限：建議 90-180 天後自動清除
```

---

## 15. 未來擴展

### Phase 2

- 回報投票（確認/質疑）機制
- 管理後台（審核回報、管理使用者）
- 瀏覽器擴充套件

### Phase 3

- Pro 付費方案（更高限額）
- Monitor mode: webhook 訂閱（地址被回報時通知）
- 與執法機關合作匯出
- 鏈上資料驗證
  - 提供超連結
  - 自動API驗證

---

## 16. 依賴套件

### Frontend

```json
{
  "dependencies": {
    "react": "^19.x",
    "react-router-dom": "^7.x",
    "react-i18next": "^15.x",
    "recharts": "^2.x",
    "tailwindcss": "^4.x",
    "@simplewebauthn/browser": "^11.x"
  },
  "devDependencies": {
    "typescript": "^5.x",
    "vite": "^6.x",
    "eslint": "^9.x",
    "@typescript-eslint/eslint-plugin": "^8.x",
    "@typescript-eslint/parser": "^8.x",
    "eslint-plugin-react-hooks": "^5.x",
    "eslint-plugin-react-refresh": "^0.4.x",
    "prettier": "^3.x",
    "vitest": "^2.x",
    "@testing-library/react": "^16.x"
  }
}
```

### Worker

```json
{
  "dependencies": {
    "hono": "^4.x",
    "@simplewebauthn/server": "^11.x",
    "nanoid": "^5.x"
  },
  "devDependencies": {
    "typescript": "^5.x",
    "wrangler": "^3.x",
    "eslint": "^9.x",
    "@typescript-eslint/eslint-plugin": "^8.x",
    "@typescript-eslint/parser": "^8.x",
    "prettier": "^3.x",
    "vitest": "^2.x",
    "@cloudflare/vitest-pool-workers": "^0.5.x"
  }
}
```

### Root (Monorepo 工具)

```json
{
  "devDependencies": {
    "prettier": "^3.x",
    "husky": "^9.x",
    "lint-staged": "^15.x"
  }
}
```

---

## 17. 免責聲明

```
本平台所提供之資訊僅供參考，不構成任何法律意見或投資建議。
所有回報內容由使用者提交，平台不對其準確性負責。
提交回報時，您的 IP 地址將被記錄以防止濫用。

The information provided on this platform is for reference only.
All reports are user-submitted. Your IP address will be recorded
when submitting reports to prevent abuse.
```

---

_文件版本 v1.4 — 最後更新 2026-02-27_
_變更：新增 SEO 策略章節（Prerender/SSR、Meta Tags、JSON-LD、Sitemap、Core Web Vitals、OG Image、hreflang）_
